
# Prefeito, vamos implementar uma nova funcionalidade no Easydrive

## 1. Objetivo da Tarefa

> (Preencha aqui o que você quer. Ex: "Criar o endpoint CRUD para o modelo `Review`.")

## 2. Contexto e Localização

> (Preencha os arquivos que serão modificados. Ex: "A implementação deve ser feita em `apps/reviews/models.py`, `apps/reviews/serializers.py` e `apps/reviews/views.py`.")

## 3. Checklist de Implementação (Regras)

Por favor, siga estas regras estritamente ao gerar o código:

* [ ] **Docstrings:** Gerar docstrings (padrão Google) para todas as novas classes, métodos e funções. **Não usar comentários inline (`#`).**
* [ ] **Tipagem (Type Hinting):** Adicionar type hints do Python em todos os argumentos de funções e retornos.
* [ ] **Testes:** Gerar um teste unitário básico para esta funcionalidade em `apps/reviews/tests/test_views.py`.
* [ ] **Performance:** Garantir que nenhum problema N+1 foi introduzido.
* [ ] **Formatação:** O código deve estar formatado com `black`.
* [ ] **Verboso vs. Conciso:** Prefira código claro e legível, evitando complexidade desnecessária.
